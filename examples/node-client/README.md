# Node.js Client Example

Demonstrates the Live Neon Persona API from Node.js: register, create an agent, bulk-add beliefs, and generate a system prompt.

## Run

```bash
node example.mjs
```

Requires Node 18+ (native fetch). No dependencies needed. No API key needed — the script registers automatically.
