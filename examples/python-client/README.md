# Python Client Example

Demonstrates the full Live Neon Persona API from Python: register, create an agent, add beliefs and responsibilities, generate a system prompt, and import a CLAUDE.md.

## Run

```bash
pip install requests
python example.py
```

No API key needed — the script registers automatically.
